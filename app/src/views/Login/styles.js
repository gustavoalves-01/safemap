import styled from 'styled-components';

export const Container = styled.main`
  background: var(--black);
  width: 100vw;
  height: 100vh;

  # {
      width: 100vw;
      height: 100vh;
      position: fixed;
      z-index: 0;
  }
`;
